# Hi-Voicer 环境准备

这份说明用于换电脑时复现开发环境。项目目录保持在 `A:\Gmini\Hi-Voicer` 这种独立目录内，Git、依赖、Tauri 配置都在项目里面。

## 需要安装的软件

1. Node.js 20 或更新版本
2. Rust 1.96.0，Windows MSVC 工具链
3. Microsoft Visual Studio 2022 Build Tools，包含 C++ Build Tools
4. Microsoft Edge WebView2 Runtime

Tauri v2 官方 Windows 前置条件是 Microsoft C++ Build Tools、Microsoft Edge WebView2 和 Rust。当前项目已经把 npm 依赖锁在 `package-lock.json`，Rust 工具链锁在 `rust-toolchain.toml`。

## 新电脑快速步骤

在 PowerShell 里进入项目目录：

```powershell
cd A:\Gmini\Hi-Voicer
```

如果已经安装好 Node.js 和 Visual Studio Build Tools，可以直接运行：

```powershell
.\scripts\setup-windows.ps1
```

如果系统禁止执行脚本，可以只对当前 PowerShell 窗口放开：

```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\scripts\setup-windows.ps1
```

## 检查环境

```powershell
.\scripts\check-env.ps1
```

应该看到这些项目通过：

```text
Node.js
npm
rustc
cargo
Visual Studio C++ Build Tools
Microsoft Edge WebView2 Runtime
```

## 常用验证命令

```powershell
npm ci
npm test
npm run build
cargo check --manifest-path src-tauri\Cargo.toml
npm run tauri -- --version
```

正式桌面包构建：

```powershell
npm run tauri -- build
```

## 当前机器已验证

- Node.js: v25.2.0
- npm: 11.6.2
- Rust: 1.96.0
- Tauri CLI: 2.11.2
- WebView2 Runtime: 已安装
- Visual Studio 2022 Build Tools: 已安装

已通过：

```text
npm test
npm run build
cargo check --manifest-path src-tauri\Cargo.toml
```
